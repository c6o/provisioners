apiVersion: v1
kind: Service
metadata:
  name: appengine
  namespace: {{namespace}}
  labels:
    app: {{name}}
    app.kubernetes.io/managed-by: codezero
    system.codezero.io/app: {{name}}
spec:
  type: NodePort
  externalTrafficPolicy: Cluster
{{servicePortContent}}
  selector:
    app: {{name}}
