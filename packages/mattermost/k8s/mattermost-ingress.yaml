apiVersion: v1
kind: Service
metadata:
  name: {{name}}-nodeport
  namespace: {{namespace}}
  labels:
    app: mattermost-cluster
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 8065
  selector:
    app: mattermost-nodeport