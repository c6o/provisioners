apiVersion: system.codezero.io/v1
kind: App
metadata:
  name: mosquitto
  namespace: testing
  labels:
    system.codezero.io/edition: latest
spec:
  provisioner:
    add-user:
      user: userName1
      password: demoPassword