apiVersion: system.codezero.io/v1
kind: App
metadata:
  name: mosquitto
  namespace: testing
  labels:
    system.codezero.io/editionId: latest
spec:
  provisioner:
    remove-user:
      user: userName