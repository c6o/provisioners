# Source: nextcloud/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: nextcloud
  namespace: '{{namespace}}'
  labels:
    role: server
    name: nextcloud
    system.codezero.io/app: nextcloud
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    name: http
  selector:
    name: nextcloud