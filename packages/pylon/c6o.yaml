name: 'pylon'         # display name
appId: pylon                   #App internal name (all lower, no spaces); minimum 5 characters
package: '@provisioner/appengine'  #the NPM package name.  Typically id and package-name are the same, but not required.

editions:
  - name: preview
    scope: public           #public|private|deprecated   public:everyone, private:org/person only
    default: true           #proposed: default edition used for installation
    spec:                   #https://docs.codezero.io/#/reference/appspec?id=spec-section
      routes:
        - type: http
          targetService: pylon
      provisioner:
        package: '@provisioner/appengine'
        ui: ignore
        name: pylon
        image: 'linuxserver/pylon:latest'
        port: 3131
        automated: true
        config:
          - name: TZ
            value: America/Vancouver
        volume:
          - name: code
            mountPath: /code
            size: 5Gi
      marina:
        launch:
          type: inline
          popUp: true
