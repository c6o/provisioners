name: 'radarr'
appId: radarr                #App internal name (all lower, no spaces); minimum 5 characters
package: '@provisioner/appengine'

editions:
  - name: preview
    scope: public
    default: true
    spec:
      routes:
        - type: http
          targetService: radarr
      provisioner:
        package: '@provisioner/appengine'
        ui: ignore
        name: radarr
        image: 'linuxserver/radarr:latest'
        port: 7878
        automated: true
        config:
          - name: TZ
            value: America/Vancouver
        volume:
          - size: 5Gi
            mountPath: /config
            name: config
          - size: 500Gi
            mountPath: /movies
            name: movies
          - size: 50Gi
            mountPath: /downloads
            name: downloads
      marina:
        launch:
          type: inline
          popUp: true
