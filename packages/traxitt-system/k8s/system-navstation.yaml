apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: 'traxitt-system' # '{{namespace}}'
  name: system-navstation
  labels:
    role: server
    name: system-navstation
spec:
  selector:
    matchLabels:
      name: system-navstation
  replicas: 1
  template:
    metadata:
      labels:
        role: server
        name: system-navstation
    spec:
      containers:
      - name: system-navstation
        image: traxitt/system-navstation:latest
        imagePullPolicy: Always
        ports:
          - containerPort: 80
---

apiVersion: v1
kind: Service
metadata:
    namespace: 'traxitt-system' # '{{namespace}}'
    name: system-navstation
    labels:
        name: system-navstation
        role: server
spec:
    ports:
    - port: 80
      name: http
    type: NodePort
    selector:
        name: system-navstation
---

apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
    name: system-navstation-virtualservice
    namespace: traxitt-system # '{{namespace}}'
spec:
    hosts:
    - 'cluster.grant.dev.traxitt.org'
    gateways:
    - traxitt-system-gateway
    http:
    - match:
      - uri:
          prefix: /navstation/
        rewrite:
            uri: /
        route:
        - destination:
            port:
              number: 80
            host: system-navstation
---        