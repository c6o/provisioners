kind: Service
apiVersion: v1
metadata:
  name: dev-pod-ssh-svc
  namespace: '{{namespace}}'
  labels:
    app: vscode
spec:
  type: NodePort
  ports:
    - protocol: TCP
      name: ssh
      port: 22
      targetPort: 22
  selector:
    app: vscode